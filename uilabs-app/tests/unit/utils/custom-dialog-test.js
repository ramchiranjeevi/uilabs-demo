import customDialog from '../../../utils/custom-dialog';
import { module, test } from 'qunit';

module('Unit | Utility | custom dialog');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = customDialog();
  assert.ok(result);
});
