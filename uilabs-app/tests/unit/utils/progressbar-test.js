import progressbar from '../../../utils/progressbar';
import { module, test } from 'qunit';

module('Unit | Utility | progressbar');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = progressbar();
  assert.ok(result);
});
