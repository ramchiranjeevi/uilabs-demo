  <div class="basicmenus">Basic Menus</div>
  <div  class="ui vertical menu" style="margin-top: 10px;">

    {{#each model as | item |}}
    {{menu-component menuItem=item content=item}}
    {{/each}}
  </div>
  <div id="moreMenu" class="moremenudiv" style="margin-top: -453px;margin-left: 370px" >
    <div {{action 'showMoreMenus'}}>More Menus</div>
  </div>
  <div id="contextMenu" class="contextmenudiv" style="margin-top: -35px;margin-left: 666px;">
    <div {{action "showContextMenus" post on="contextMenu" }} {{action "hideContextMenu" post on="click"}}>Context Menus</div>
  </div>
{{#if isMoreMenu}}
<div  class="ui vertical menu" style="position: absolute;margin-top: -1px;margin-left: 367px;">
  {{#each model as | item |}}
  {{menu-component menuItem=item content=item}}
  {{/each}}
</div>
{{/if}}
{{#if isContextMenu}}
<div  class="ui vertical menu" style="position: absolute;margin-top: -7px;margin-left: 760px;">
  {{#each model as | item |}}
  {{menu-component menuItem=item content=item}}
  {{/each}}
</div>
{{/if}}
