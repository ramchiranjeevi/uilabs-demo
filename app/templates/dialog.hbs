<div>
    <button class="ui button green" {{action 'showCreateNewDialog'}}> Create New Folder </button>
    <button class="ui button blue" {{action 'showUnzipDialog'}}> Unzip </button>
</div>
<br>
<br>
{{#if showDialog}}
    {{#ui-dialog dialogObject=dialogObject}}
        {{component componentToBeRendered}}
    {{/ui-dialog}}
{{/if}}
